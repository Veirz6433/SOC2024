#include<stdio.h>
bool isPowerOfTwo(int n) {
    if(n <=0) //negative numbers are 0 and not power of two
    return false;
    return (n & (n - 1)) == 0;

    if(isPowerOfTwo(n))
    printf("%d true\n", n);
    else
    printf("%d false.\n", n);
    return 0;
}
