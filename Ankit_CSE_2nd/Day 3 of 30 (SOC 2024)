class Solution {
public:
    void sortColors(vector<int>& nums) {
      multiset<int> s;
      for(int i=0;i<nums.size();i++){
        s.insert(nums[i]);
        }
        int i=0;
      for(int value: s){
        nums[i]=value;
        i++;
      }
 }
};
